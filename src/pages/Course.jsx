import React from 'react'
import Container from '../components/Container'
// Mock data
import CourseList from '../mockCourses'

export default function Course(props) {
	// const course = CourseList.find((item) => item.url === props.match.url)
    return (
			<Container>
				Course
			</Container>
	)
}